---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    anthropic.crossplane.io/sdk-version: v1.71.0
  name: federationrules.organization.anthropic.crossplane.io
spec:
  group: organization.anthropic.crossplane.io
  names:
    categories:
      - crossplane
      - managed
      - anthropic
    kind: FederationRule
    listKind: FederationRuleList
    plural: federationrules
    shortNames:
      - fdrl
    singular: federationrule
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.conditions[?(@.type=='Ready')].status
          name: READY
          type: string
        - jsonPath: .status.conditions[?(@.type=='Synced')].status
          name: SYNCED
          type: string
        - jsonPath: .metadata.annotations.crossplane\.io/external-name
          name: EXTERNAL-NAME
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: AGE
          type: date
      name: v1beta1
      schema:
        openAPIV3Schema:
          description: |-
            FederationRule is a managed resource representing a workload identity
            federation rule in an Anthropic organization (Admin API): which issuer's
            tokens, matching which claims, may be exchanged for which service
            account's access tokens. The endpoint accepts only org:admin OAuth tokens,
            so it requires a ProviderConfig with the WorkloadIdentityFederation
            identity.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: FederationRuleSpec defines the desired state of FederationRule.
              properties:
                forProvider:
                  description: |-
                    ForProvider holds the configuration the provider reconciles against the
                    Anthropic API on every loop.
                  properties:
                    appliesToAllWorkspaces:
                      description: |-
                        AppliesToAllWorkspaces enables the rule for every workspace, including
                        ones created later.
                      type: boolean
                    description:
                      description: Description is free text.
                      type: string
                    issuerId:
                      description: |-
                        Required: IssuerID is the FederationIssuer whose tokens this rule
                        accepts. Populate directly or via IssuerIDRef / IssuerIDSelector.
                      type: string
                    issuerIdRef:
                      description: Reference to a FederationIssuer to populate issuerId.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                                - Required
                                - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                                - Always
                                - IfNotPresent
                              type: string
                          type: object
                      required:
                        - name
                      type: object
                    issuerIdSelector:
                      description: Selector for a FederationIssuer to populate issuerId.
                      properties:
                        matchControllerRef:
                          description: |-
                            MatchControllerRef ensures an object with the same controller reference
                            as the selecting object is selected.
                          type: boolean
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: MatchLabels ensures an object with matching labels is selected.
                          type: object
                        namespace:
                          description: Namespace for the selector
                          type: string
                        policy:
                          description: Policies for selection.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                                - Required
                                - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                                - Always
                                - IfNotPresent
                              type: string
                          type: object
                      type: object
                    match:
                      description: 'Required: Match lists the conditions the verified JWT must satisfy.'
                      properties:
                        audience:
                          description: |-
                            Audience matches the aud claim (any element if it is an array). When
                            omitted the aud must equal Anthropic's expected audience for the
                            issuer; setting it overrides that default.
                          type: string
                        claims:
                          additionalProperties:
                            type: string
                          description: Claims are exact-match pairs against top-level string claims.
                          type: object
                        condition:
                          description: |-
                            Condition is a CEL expression over the claims variable for logic the
                            structural fields cannot express; a constant-true expression is
                            rejected.
                          type: string
                        subjectPrefix:
                          description: |-
                            SubjectPrefix matches the sub claim exactly, or as a prefix when the
                            value ends with "*", for example
                            "system:serviceaccount:crossplane-system:provider-anthropic-*".
                          type: string
                      type: object
                    name:
                      description: |-
                        Required: Name is a slug (lowercase, digits, hyphens), unique within
                        the organization; a duplicate returns 409.
                      pattern: ^[a-z0-9][a-z0-9-]*$
                      type: string
                    oauthScope:
                      description: |-
                        Required: OAuthScope is the space-separated scopes of minted tokens.
                        Rules created through the API may grant workspace:developer or
                        workspace:inference; org:admin requires a Console session.
                      type: string
                    serviceAccountId:
                      description: |-
                        Required: ServiceAccountID is the ServiceAccount that tokens minted via
                        this rule act as. Populate directly or via ServiceAccountIDRef /
                        ServiceAccountIDSelector.
                      type: string
                    serviceAccountIdRef:
                      description: Reference to a ServiceAccount to populate serviceAccountId.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                                - Required
                                - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                                - Always
                                - IfNotPresent
                              type: string
                          type: object
                      required:
                        - name
                      type: object
                    serviceAccountIdSelector:
                      description: Selector for a ServiceAccount to populate serviceAccountId.
                      properties:
                        matchControllerRef:
                          description: |-
                            MatchControllerRef ensures an object with the same controller reference
                            as the selecting object is selected.
                          type: boolean
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: MatchLabels ensures an object with matching labels is selected.
                          type: object
                        namespace:
                          description: Namespace for the selector
                          type: string
                        policy:
                          description: Policies for selection.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                                - Required
                                - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                                - Always
                                - IfNotPresent
                              type: string
                          type: object
                      type: object
                    tokenLifetimeSeconds:
                      description: |-
                        TokenLifetimeSeconds is the lifetime of minted access tokens (60 to
                        86400). The API default is 3600.
                      format: int64
                      maximum: 86400
                      minimum: 60
                      type: integer
                    workspaceId:
                      description: |-
                        WorkspaceID enables the rule for one workspace. Required unless
                        AppliesToAllWorkspaces is true. Populate directly or via
                        WorkspaceIDRef / WorkspaceIDSelector.
                      type: string
                    workspaceIdRef:
                      description: Reference to a Workspace to populate workspaceId.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                                - Required
                                - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                                - Always
                                - IfNotPresent
                              type: string
                          type: object
                      required:
                        - name
                      type: object
                    workspaceIdSelector:
                      description: Selector for a Workspace to populate workspaceId.
                      properties:
                        matchControllerRef:
                          description: |-
                            MatchControllerRef ensures an object with the same controller reference
                            as the selecting object is selected.
                          type: boolean
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: MatchLabels ensures an object with matching labels is selected.
                          type: object
                        namespace:
                          description: Namespace for the selector
                          type: string
                        policy:
                          description: Policies for selection.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                                - Required
                                - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                                - Always
                                - IfNotPresent
                              type: string
                          type: object
                      type: object
                  type: object
                managementPolicies:
                  default:
                    - '*'
                  description: |-
                    THIS IS A BETA FIELD. It is on by default but can be opted out
                    through a Crossplane feature flag.
                    ManagementPolicies specify the array of actions Crossplane is allowed to
                    take on the managed and external resources.
                    See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                    and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                  items:
                    description: |-
                      A ManagementAction represents an action that the Crossplane controllers
                      can take on an external resource.
                    enum:
                      - Observe
                      - Create
                      - Update
                      - Delete
                      - LateInitialize
                      - '*'
                    type: string
                  type: array
                providerConfigRef:
                  default:
                    kind: ClusterProviderConfig
                    name: default
                  description: |-
                    ProviderConfigReference specifies how the provider that will be used to
                    create, observe, update, and delete this managed resource should be
                    configured.
                  properties:
                    kind:
                      description: Kind of the referenced object.
                      type: string
                    name:
                      description: Name of the referenced object.
                      type: string
                  required:
                    - kind
                    - name
                  type: object
                writeConnectionSecretToRef:
                  description: |-
                    WriteConnectionSecretToReference specifies the namespace and name of a
                    Secret to which any connection details for this managed resource should
                    be written. Connection details frequently include the endpoint, username,
                    and password required to connect to the managed resource.
                  properties:
                    name:
                      description: Name of the secret.
                      type: string
                  required:
                    - name
                  type: object
              required:
                - forProvider
              type: object
            status:
              description: FederationRuleStatus defines the observed state of FederationRule.
              properties:
                atProvider:
                  description: AtProvider holds the observed state as returned by the Anthropic API.
                  properties:
                    appliesToAllWorkspaces:
                      description: AppliesToAllWorkspaces is the observed all-workspaces flag.
                      type: boolean
                    archivedAt:
                      description: ArchivedAt is set when the rule has been archived.
                      type: string
                    createdAt:
                      description: CreatedAt is the RFC 3339 timestamp when the rule was created.
                      type: string
                    createdByActorId:
                      description: CreatedByActorID identifies who created the rule.
                      type: string
                    description:
                      description: Description is the observed description.
                      type: string
                    id:
                      description: |-
                        ID is the Anthropic-assigned rule identifier (fdrl_...). Also stored
                        in the external-name annotation, which the reconciler uses as the
                        primary key.
                      type: string
                    issuerId:
                      description: IssuerID is the observed issuer.
                      type: string
                    issuerName:
                      description: IssuerName is the observed issuer slug.
                      type: string
                    match:
                      description: Match is the observed match configuration.
                      properties:
                        audience:
                          description: |-
                            Audience matches the aud claim (any element if it is an array). When
                            omitted the aud must equal Anthropic's expected audience for the
                            issuer; setting it overrides that default.
                          type: string
                        claims:
                          additionalProperties:
                            type: string
                          description: Claims are exact-match pairs against top-level string claims.
                          type: object
                        condition:
                          description: |-
                            Condition is a CEL expression over the claims variable for logic the
                            structural fields cannot express; a constant-true expression is
                            rejected.
                          type: string
                        subjectPrefix:
                          description: |-
                            SubjectPrefix matches the sub claim exactly, or as a prefix when the
                            value ends with "*", for example
                            "system:serviceaccount:crossplane-system:provider-anthropic-*".
                          type: string
                      type: object
                    name:
                      description: Name is the observed slug.
                      type: string
                    oauthScope:
                      description: OAuthScope is the observed scope of minted tokens.
                      type: string
                    serviceAccountId:
                      description: ServiceAccountID is the observed target service account.
                      type: string
                    serviceAccountName:
                      description: ServiceAccountName is the observed target service account name.
                      type: string
                    tokenLifetimeSeconds:
                      description: TokenLifetimeSeconds is the observed token lifetime.
                      format: int64
                      type: integer
                    updatedAt:
                      description: UpdatedAt is the RFC 3339 timestamp of the last modification.
                      type: string
                    updatedByActorId:
                      description: UpdatedByActorID identifies who last modified the rule.
                      type: string
                    workspaceId:
                      description: WorkspaceID is the observed primary workspace binding.
                      type: string
                    workspaceIds:
                      description: WorkspaceIDs lists every workspace the rule is enabled for.
                      items:
                        type: string
                      type: array
                  type: object
                conditions:
                  description: Conditions of the resource.
                  items:
                    description: A Condition that may apply to a resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          LastTransitionTime is the last time this condition transitioned from one
                          status to another.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          A Message containing details about this condition's last transition from
                          one status to another, if any.
                        type: string
                      observedGeneration:
                        description: |-
                          ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        type: integer
                      reason:
                        description: A Reason for this condition's last transition from one status to another.
                        type: string
                      status:
                        description: Status of this condition; is it currently True, False, or Unknown?
                        type: string
                      type:
                        description: |-
                          Type of this condition. At most one of each condition type may apply to
                          a resource at any point in time.
                        type: string
                    required:
                      - lastTransitionTime
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                lastHandledReconcileAt:
                  description: |-
                    LastHandledReconcileAt holds the value of the most recent
                    reconcile-requested-at annotation token that the controller has
                    processed. Users can compare this to the annotation to determine
                    whether a reconcile request has been handled.
                  type: string
                observedGeneration:
                  description: |-
                    ObservedGeneration is the latest metadata.generation
                    which resulted in either a ready state, or stalled due to error
                    it can not recover from without human intervention.
                  format: int64
                  type: integer
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
