---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
    anthropic.crossplane.io/sdk-version: v1.43.0
  name: observedagentcollections.beta.anthropic.crossplane.io
spec:
  group: beta.anthropic.crossplane.io
  names:
    categories:
      - crossplane
      - managed
      - anthropic
    kind: ObservedAgentCollection
    listKind: ObservedAgentCollectionList
    plural: observedagentcollections
    shortNames:
      - obscoll-agent
    singular: observedagentcollection
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.conditions[?(@.type=='Ready')].status
          name: READY
          type: string
        - jsonPath: .status.conditions[?(@.type=='Synced')].status
          name: SYNCED
          type: string
        - jsonPath: .status.itemCount
          name: ITEMS
          type: integer
        - jsonPath: .metadata.creationTimestamp
          name: AGE
          type: date
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: |-
            ObservedAgentCollection materializes one Observe-only Agent per remote
            agent matched by spec.predicates.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: |-
                ObservedAgentCollectionSpec defines the desired state of
                ObservedAgentCollection.
              properties:
                predicates:
                  description: |-
                    Predicates filter the Anthropic List(agents) call. The reconciler maps
                    fields to BetaAgentListParams when supported server-side and applies
                    any remaining predicates client-side.
                  properties:
                    celFilter:
                      description: |-
                        CELFilter is a CEL expression that must evaluate to a boolean. The
                        expression receives the JSON-decoded API response item as the variable
                        "atProvider" (map[string]any). Use dot notation for field access:
                        atProvider.name == "foo" or atProvider.metadata.key == "val".
                        For keys that contain special characters (e.g. hyphens) bracket notation
                        is still required: atProvider["my-key"] == "val".
                        Resources for which the expression evaluates to false are excluded.
                      type: string
                    createdAtGte:
                      description: |-
                        CreatedAtGte matches resources created at or after this RFC3339
                        timestamp (inclusive).
                      format: date-time
                      type: string
                    createdAtLte:
                      description: |-
                        CreatedAtLte matches resources created at or before this RFC3339
                        timestamp (inclusive).
                      format: date-time
                      type: string
                    metadataMatch:
                      additionalProperties:
                        type: string
                      description: |-
                        MetadataMatch retains only resources whose API metadata map contains
                        every specified key-value pair. Resources with no metadata, or missing
                        any key, are excluded. An empty map passes all resources.
                      type: object
                  type: object
                providerConfigRef:
                  default:
                    kind: ClusterProviderConfig
                    name: default
                  description: |-
                    ProviderConfigReference selects the ProviderConfig used to authenticate
                    against the Anthropic API for this collection.
                  properties:
                    kind:
                      description: Kind of the referenced object.
                      type: string
                    name:
                      description: Name of the referenced object.
                      type: string
                  required:
                    - kind
                    - name
                  type: object
                template:
                  description: Template is applied to every Agent child created by this collection.
                  properties:
                    metadata:
                      description: |-
                        ObservedCollectionTemplateMetadata is the label/annotation overlay applied
                        to children.
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          type: object
                        labels:
                          additionalProperties:
                            type: string
                          type: object
                      type: object
                  type: object
              type: object
            status:
              description: |-
                ObservedAgentCollectionStatus defines the observed state of
                ObservedAgentCollection.
              properties:
                conditions:
                  description: Conditions of the resource.
                  items:
                    description: A Condition that may apply to a resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          LastTransitionTime is the last time this condition transitioned from one
                          status to another.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          A Message containing details about this condition's last transition from
                          one status to another, if any.
                        type: string
                      observedGeneration:
                        description: |-
                          ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        type: integer
                      reason:
                        description: A Reason for this condition's last transition from one status to another.
                        type: string
                      status:
                        description: Status of this condition; is it currently True, False, or Unknown?
                        type: string
                      type:
                        description: |-
                          Type of this condition. At most one of each condition type may apply to
                          a resource at any point in time.
                        type: string
                    required:
                      - lastTransitionTime
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                itemCount:
                  description: |-
                    ItemCount mirrors len(Items) for use in a printer column (JSONPath
                    does not support array length).
                  format: int32
                  type: integer
                items:
                  description: Items references children currently materialized by this collection.
                  items:
                    description: ObservedCollectionMember is one matched child managed resource.
                    properties:
                      id:
                        description: ID is the Anthropic-assigned identifier of the observed resource.
                        type: string
                      name:
                        description: Name of the child resource in the same namespace.
                        type: string
                    required:
                      - id
                      - name
                    type: object
                  type: array
                membershipLabel:
                  additionalProperties:
                    type: string
                  description: |-
                    MembershipLabel is the label applied to every child Agent owned by
                    this collection. Useful for `kubectl get agents -l <key>=<value>` queries.
                  type: object
                observedGeneration:
                  description: |-
                    ObservedGeneration is the latest metadata.generation
                    which resulted in either a ready state, or stalled due to error
                    it can not recover from without human intervention.
                  format: int64
                  type: integer
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources:
        status: {}
